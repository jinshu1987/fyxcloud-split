Rule IDRule NameRule DescriptionApplicabilityIAM-046Short-Lived Token EnforcementDetect AI Agent service accounts or IAM roles using long-lived access keys instead of temporary, session-based credentials (STS).Multi-CloudIAM-047Agent S3 Prefix RestrictionAlert if an AI Agent's IAM policy allows s3:GetObject on an entire bucket instead of a specific subdirectory (prefix) designated for that agent.AWS / GCPIAM-048MFA for Custom Model DeletionFlag any IAM policy that allows DeleteModel or DeleteResolver actions without an explicit MultiFactorAuthPresent: true condition.AWS / AzureIAM-049Agent-to-Secrets Manager LeakDetect AI execution roles that have SecretsManager:GetSecretValue permissions for secrets not tagged with the agent's unique ID.AWS / AzureIAM-050Cross-Account Agent AssumptionAlert on "AssumeRole" trust relationships that allow AI agents from external/untrusted accounts to invoke models in the production environment.AWSIAM-051Excessive Agent "Write" PermsFlag AI Agents with Update, Delete, or Put permissions on production databases (RDS/DynamoDB) without an approval-flow middleware.Multi-CloudIAM-052Wildcard Model InvocationDetect IAM policies using bedrock:InvokeModel on Resource: *. Agents should be restricted to specific approved Model ARNs.AWSIAM-053Unused AI Agent IdentitiesIdentify AI-specific Service Accounts or Roles that have not performed an Invoke or Training action in the last 30 days (Ghost Agents).Multi-CloudIAM-054Agent Boundary EnforcementDetect AI roles missing an IAM Permission Boundary. Boundaries prevent agents from escalating their own permissions via prompt injection.AWSIAM-055Shadow Agent CreationAlert when a non-admin user creates a new Service Principal with "AI Developer" or "Vertex AI User" roles assigned.GCP / AzureIAM-056Interactive Login for AgentsFlag AI Service Accounts that have "Interactive Login" enabled. Agents should only authenticate via non-interactive API keys or tokens.Azure / GCPIAM-057Agent Metadata Access (SSRF)Detect AI Agents running on EC2/Compute Engine that have access to the Instance Metadata Service (IMDSv1), which is vulnerable to SSRF.AWS / GCPIAM-058Insecure Agent Key StorageScan Agent configuration files (e.g., config.yaml or .env) in cloud storage for plaintext AI API keys or Bearer tokens.Multi-CloudIAM-059Over-scoped "Search" PermissionsAlert if an AI Agent used for RAG (Retrieval) has Search permissions across indices it does not need for its specific task.Elastic / KendraIAM-060Agent Privilege Escalation via LambdaDetect AI Agents with lambda:UpdateFunctionCode permissions, which could allow an injected prompt to rewrite the agent's own logic.AWS